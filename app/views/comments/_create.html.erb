<%= form_with model:[@recipe, @recipe.comments.build], method: :post, local: true do |f| %>
  <%= f.label :text, "Comment:" %> <br>
  <%= f.text_area :text, size:"40x5", placeholder: "Add comment...", required: true %>
  <br>
  <%= f.submit "Submit" %>
<% end %>
